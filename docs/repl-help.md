# REPL help

Enter `#help` at the REPL prompt to display the core term, definition, theorem,
and exit syntax, with examples that can be entered directly. The command does
not read or change session definitions and does not accept arguments.
